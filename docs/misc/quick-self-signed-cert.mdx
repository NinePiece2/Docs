---
title: Self-Signed Certificates
description: Some quick commands for creating self-signed certificates
---

## Create a self-signed keypair in the current directory

_Creates a self-signed keypair in the current directory for the common name "localhost" that expires in 10 years._

```bash
openssl req -x509 -newkey rsa:2048 -keyout ssl.key -out ssl.crt -days 36500 -nodes -subj "/CN=localhost"
```
